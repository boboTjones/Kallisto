#nav{:class => @active}

.nav
  %ul
    - @lm.logfiles["/var/log"].each do |logfile|
      - fn = logfile.gsub(".","_")
      %li{:class => "#{fn}"}
        %a{:href => "/logs/show/#{fn}"} 
          = logfile